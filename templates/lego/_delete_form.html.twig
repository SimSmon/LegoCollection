<form method="post" action="{{ path('app_lego_delete', {'id': lego.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lego.id) }}">
    <button class="btn">Delete</button>
</form>
