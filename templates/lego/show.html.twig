{% extends 'base.html.twig' %}

{% block title %}Lego{% endblock %}

{% block body %}
    <h1>{{ lego.name }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Ref</th>
                <td>{{ lego.ref }}</td>
            </tr>
            <tr>
                <th>theme</th>
                <td>{{ lego.theme.name }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ lego.year }}</td>
            </tr>
            <tr>
                <th>Nombre de pieces</th>
                <td>{{ lego.numParts }}</td>
            </tr>
        </tbody>
    </table>

    <div class="">
        <img src="{{ lego.imageURL }}" />
    </div>

    <a href="{{ path('app_lego_index') }}">back to list</a>

    <a href="{{ path('app_lego_edit', {'id': lego.id}) }}">edit</a>

    {{ include('lego/_delete_form.html.twig') }}
{% endblock %}
